VERIFIED
⚠ Deviated from the runbook: Used jsonpath waiting for .status.healthy and added IAM binding retry loops for propagation delay

## Attempt Log

### Attempt 1
- **What failed**: 
  1. GKE cluster creation command exceeded the shell execution timeout during initial cluster provisioning.
  2. `gcloud projects add-iam-policy-binding` failed immediately after `gcloud iam service-accounts create` with `INVALID_ARGUMENT: Service account kcc-instance1-sa@barni-cnrm-20260529.iam.gserviceaccount.com does not exist` due to GCP IAM backend propagation latency.
- **Evidence**:
  ```text
  ERROR: (gcloud.projects.add-iam-policy-binding) INVALID_ARGUMENT: Service account kcc-instance1-sa@barni-cnrm-20260529.iam.gserviceaccount.com does not exist.
  ```
- **What changed**:
  - Added a retry loop (up to 6 attempts with 5s delay) to `gcloud projects add-iam-policy-binding` and `gcloud iam service-accounts add-iam-policy-binding` in `deploy.sh`.

### Attempt 2
- **What failed**:
  - `kubectl wait --for=condition=Healthy=true configconnector/configconnector.core.cnrm.cloud.google.com` timed out after 180s even though the operator, controllers, and CRs were fully functional and ready.
- **Evidence**:
  - `kubectl get configconnector` showed `.status.healthy: true` (a boolean field), whereas `--for=condition=Healthy=true` checks `.status.conditions[?(@.type=="Healthy")].status=="True"` which is not populated on `ConfigConnector` CRs.
- **What changed**:
  - Updated `deploy.sh` verification commands to use `kubectl wait --for=jsonpath='{.status.healthy}'=true` for both `ConfigConnector` and `ConfigConnectorContext`.

### Attempt 3
- **Execution**: Succeeded deterministically end-to-end. Operator, per-namespace controller manager, webhook manager, deletion defender, and unmanaged detector all started and reconciled. The test `StorageBucket` `kcc-instance1-bucket` reached `Ready=true` (`UpToDate`) on Kubernetes and was confirmed created in GCP Cloud Storage.

---

## Verification Evidence

### 1. Operator and System Pods
```text
NAMESPACE                         NAME                                            READY   STATUS    RESTARTS   AGE
configconnector-operator-system   configconnector-operator-0                      1/1     Running   0          4m
cnrm-system                       cnrm-controller-manager-pvbuk2e5syzmjhq-0       2/2     Running   4          4m
cnrm-system                       cnrm-deletiondefender-0                         1/1     Running   0          3m
cnrm-system                       cnrm-resource-stats-recorder-57d4484559-4nmhh   2/2     Running   0          3m
cnrm-system                       cnrm-unmanaged-detector-0                       1/1     Running   0          3m
cnrm-system                       cnrm-webhook-manager-6845cbcb5c-bsjjz           1/1     Running   0          3m
cnrm-system                       cnrm-webhook-manager-6845cbcb5c-rzr2m           1/1     Running   0          3m
```

### 2. ConfigConnector and ConfigConnectorContext Health
```yaml
apiVersion: core.cnrm.cloud.google.com/v1beta1
kind: ConfigConnector
metadata:
  name: configconnector.core.cnrm.cloud.google.com
spec:
  mode: namespaced
  stateIntoSpec: Absent
status:
  healthy: true
  observedGeneration: 1
---
apiVersion: core.cnrm.cloud.google.com/v1beta1
kind: ConfigConnectorContext
metadata:
  name: configconnectorcontext.core.cnrm.cloud.google.com
  namespace: kcc-instance1-control
spec:
  googleServiceAccount: kcc-instance1-sa@barni-cnrm-20260529.iam.gserviceaccount.com
  stateIntoSpec: Absent
status:
  healthy: true
  observedGeneration: 1
```

### 3. End-to-End Test Resource Reconciliation (`StorageBucket`)
```yaml
apiVersion: storage.cnrm.cloud.google.com/v1beta1
kind: StorageBucket
metadata:
  annotations:
    cnrm.cloud.google.com/project-id: barni-cnrm-20260529
  labels:
    repo-agent-instance: kcc-instance1
  name: kcc-instance1-bucket
  namespace: kcc-instance1-control
spec:
  lifecycleRule:
  - action:
      type: Delete
    condition:
      age: 1
      withState: ANY
  location: us-central1
  resourceID: kcc-instance1-bucket
  uniformBucketLevelAccess: true
status:
  conditions:
  - lastTransitionTime: "2026-09-24T21:29:44Z"
    message: The resource is up to date
    reason: UpToDate
    status: "True"
    type: Ready
  observedGeneration: 2
  observedState:
    softDeletePolicy:
      effectiveTime: "2026-09-24T21:29:44.497Z"
      retentionDurationSeconds: 604800
    storageClass: STANDARD
  selfLink: https://www.googleapis.com/storage/v1/b/kcc-instance1-bucket
  url: gs://kcc-instance1-bucket
```

### 4. Live GCP Cloud Storage Verification
```text
creation_time: 2026-09-24T21:29:44+0000
default_storage_class: STANDARD
generation: 1790285384277977177
labels:
  managed-by-cnrm: 'true'
  repo-agent-instance: kcc-instance1
lifecycle_config:
  rule:
  - action:
      type: Delete
    condition:
      age: 1
location: US-CENTRAL1
location_type: region
metageneration: 1
name: kcc-instance1-bucket
public_access_prevention: inherited
soft_delete_policy:
  effectiveTime: '2026-09-24T21:29:44.497000+00:00'
  retentionDurationSeconds: '604800'
storage_url: gs://kcc-instance1-bucket/
uniform_bucket_level_access: true
update_time: 2026-09-24T21:29:44+0000
```

---

## What is Left Running (Teardown Contract)

The following resources are currently active and owned by this instance (`kcc-instance1`):

1. **GKE Cluster**: `kcc-instance1-cluster` in `us-central1-a` (2 nodes `e2-standard-4`, Workload Identity pool `barni-cnrm-20260529.svc.id.goog`, label `repo-agent-instance=kcc-instance1`).
2. **Google Service Account**: `kcc-instance1-sa@barni-cnrm-20260529.iam.gserviceaccount.com` (roles: `roles/editor` on project `barni-cnrm-20260529`, `roles/iam.workloadIdentityUser` bound to `barni-cnrm-20260529.svc.id.goog[cnrm-system/cnrm-controller-manager-kcc-instance1-control]`).
3. **Config Connector Operator**: StatefulSet `configconnector-operator` running in `configconnector-operator-system`.
4. **Config Connector Namespaced Components**: Namespace `kcc-instance1-control`, controller manager `cnrm-controller-manager-pvbuk2e5syzmjhq-0` in `cnrm-system`, CRDs, webhook manager, unmanaged detector, and deletion defender.
5. **Reconciled GCP Cloud Storage Bucket**: `gs://kcc-instance1-bucket/` in region `us-central1`.

To tear down all created infrastructure, execute:
```bash
bash docs-exploration/runbook-deployments/instance1/teardown.sh
```

---

## Deviations from the Runbook

- **Retry logic on IAM policy bindings**: GCP IAM has eventual consistency propagation delay immediately following service account creation (`INVALID_ARGUMENT: Service account does not exist`). Handled via 5-second retry loops in `deploy.sh`. (From Attempt 1)
- **Wait condition syntax for ConfigConnector resources**: `ConfigConnector` and `ConfigConnectorContext` expose `.status.healthy: true` (a boolean property) rather than a conditions array. Used `kubectl wait --for=jsonpath='{.status.healthy}'=true` instead of `--for=condition=Healthy=true`. (From Attempt 2)
- **Operator deletion patch handling in teardown**: `operator/config/manager/kustomization.yaml` expects `manager_image_patch.yaml`. Updated `teardown.sh` to generate the patch template prior to `kubectl delete -k operator/config/default`.

---

## Recommended Runbook Changes

### 1. Fix Health Verification Wait Syntax (in Section `## Verify` -> `### 2. Verify ConfigConnector and ConfigConnectorContext Health`)

Replace:
```bash
kubectl wait --for=condition=Healthy=true configconnector/configconnector.core.cnrm.cloud.google.com --timeout=180s
kubectl wait -n "${KCC_NAMESPACE}" --for=condition=Healthy=true configconnectorcontext/configconnectorcontext.core.cnrm.cloud.google.com --timeout=180s
```

With:
```bash
kubectl wait --for=jsonpath='{.status.healthy}'=true configconnector/configconnector.core.cnrm.cloud.google.com --timeout=180s
kubectl wait -n "${KCC_NAMESPACE}" --for=jsonpath='{.status.healthy}'=true configconnectorcontext/configconnectorcontext.core.cnrm.cloud.google.com --timeout=180s
```

### 2. Add IAM Propagation Delay Note / Retries (in Section `## Steps` -> `### 2. Create Google Service Account`)

When creating a Google Service Account immediately before adding IAM policy bindings, allow 5–10 seconds or add retry logic to accommodate IAM replication latency across GCP control plane nodes.
